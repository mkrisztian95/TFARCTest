import CombinePlus
import Depin

class StartupService {

    func start() -> AnyPublisher<Void, Never> {
        /// Perform data requests or transitions before the app starts.
        /// Currently Nothing to be performed.

        .createJust()
    }

}
